
import BaseBusiness = require("./base/BaseService");
import IHeroModel = require("./../../model/interfaces/HeroModel");

interface HeroService extends BaseBusiness<IHeroModel> {
    
} 
export = HeroService;
